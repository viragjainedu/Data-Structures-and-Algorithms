#include<iostream>
#include<bits/stdc++.h>
#define long long ll;
using namespace std; 

int main(){
	stack<int> s ;
	// push and pop should be maintained in maxium stack thats why we dont store actual value in stack..we store (2*x - max) and change push and pop accordingly
	return 0; 
}